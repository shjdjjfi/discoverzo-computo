# Author metadata to complete before submission

The source uses the verified ORCID supplied for Zichen Song and the affiliation `Lanzhou University`. Before submitting to Computo:

1. Replace `replace-with-github-user` in `_quarto.yml` and `main.qmd`.
2. Add the author's preferred institutional email and set `corresponding: true` in `main.qmd`.
3. Replace the draft date if required by the submission system.
4. Keep `draft: true`, `published: false`, and omit a DOI until the editorial office supplies final publication metadata.
